<div class="container">
<main>
  <h1><%= t('.city_header') %></h1>
  
  <% @cities.each do |c| %>
  	 <%= c.state %> <br>
 <p class="tab"><%= link_to c.name, city_path(c.id) %></p><br> 
 <% end %>
</main>
</div>